# Brand source artwork

The original logo files supplied by the business, kept for provenance: the
vectors in `public/brand/` were traced from `db_finco_logo_enhanced.pdf`.

These live outside `public/` deliberately. Anything under `public/` is served
at the site's root, and there is no reason to publish 2 MB of source artwork
alongside the site — the delivered logos are the SVGs.
